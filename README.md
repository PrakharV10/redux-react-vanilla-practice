# redux-react-vanilla-practice

## Installation guide
- Clone the repository
- Install necessary Packages by `npm install` 
- Run the dev server using `npm run dev`
- For Generating Build, use `npm build` 
